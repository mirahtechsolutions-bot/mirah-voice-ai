{
  "name": "mirah-voice",
  "version": "1.0.0",
  "description": "AI-powered voice interview helper with TypeScript React and Python backend",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "build": "cd frontend && npm install && npm run build",
    "build-frontend": "cd frontend && npm run build",
    "type-check": "cd frontend && npm run type-check",
    "setup-ollama": "cd backend && python setup_ollama.py"
  },
  "keywords": ["ai", "voice", "interview", "typescript", "react", "python", "ollama"],
  "author": "MirahTech",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
